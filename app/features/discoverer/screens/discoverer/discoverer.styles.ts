import {COLORS} from "@features/common/constants/colors"
import {StyleSheet} from "react-native"

export const discovererStyles = StyleSheet.create({
  container: {
    backgroundColor: COLORS.WHITE,
    flex: 1,
  },
})
