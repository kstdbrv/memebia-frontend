import {StyleSheet} from "react-native"

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginBottom: 33,
    marginTop: 16,
  },
  profilePhoto: {
    borderRadius: 50,
    height: 64,
    marginBottom: 24,
    width: 64,
  },
})
