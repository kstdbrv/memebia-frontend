import {COLORS} from "../../../app/features/common/constants/colors"
import {StyleSheet} from "react-native"

export const centerViewStyles = StyleSheet.create({
  main: {
    alignItems: "center",
    backgroundColor: COLORS.WHITE,
    flex: 1,
    justifyContent: "center",
  },
})
